apply plugin: 'com.android.library'
android {
    compileSdkVersion 25
    buildToolsVersion '25.0.3'
    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 25
    }
    lintOptions {
        abortOnError false
    }
    productFlavors {
    }
}
// Used to push in maven
dependencies {
}